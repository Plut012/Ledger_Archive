# LLM Configuration - Choose ONE provider:

# Option 1: Anthropic Claude (recommended)
ANTHROPIC_API_KEY=your_anthropic_key_here
# ANTHROPIC_MODEL=claude-3-haiku-20240307  # Optional, defaults to Haiku (available on all tiers)
# Note: Sonnet/Opus require higher tier API access. Use Haiku for most use cases.

# Option 2: OpenAI
# OPENAI_API_KEY=your_openai_key_here
# OPENAI_MODEL=gpt-4  # Optional, defaults to gpt-4

# Option 3: Custom OpenAI-compatible API (Ollama, etc.)
# OPENAI_API_KEY=dummy_key_for_local
# OPENAI_BASE_URL=http://localhost:11434/v1
# OPENAI_MODEL=llama2

# MongoDB Configuration
# Defaults to mongodb://localhost:27017
# MONGO_URI=mongodb://localhost:27017

# LLM Provider Selection
# Set to 'anthropic', 'openai', or 'openai-compatible'
LLM_PROVIDER=anthropic
